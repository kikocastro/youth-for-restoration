{% extends 'master.twig' %}

{% block bodyclass %}
    no-sidebar
{% endblock bodyclass %}

{% block main %}
    <article id="main">

        <section class="wrapper style4 container">
            {% if record.image is not empty %}
                <div class="image featured">
                    <img src="{{ image(record.image) }}" alt="" />
                </div>
            {% endif %}
            <div class="content">
                <section>
                    {{ record.message }}
                </section>
            </div>
        </section>

    </article>
{% endblock main %}
